"""Generated gRPC stubs land here after `make proto`.

Until then this package is empty and the gRPC server stays disabled (the /media loop
falls back to Twilio's <Parameter> values for call context).
"""
